/**
 * layers/traffic.js
 * Phase 5 — OpenStreetMap road network + vehicle particle system.
 * Stub for now; implemented in Phase 5.
 */

export async function initTraffic(viewer) {
  console.info('[Traffic] Layer reserved for Phase 5');
  return {
    setEnabled(_val) {},
    get count() { return 0; },
  };
}
